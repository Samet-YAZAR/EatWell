trigger:
  - master
  
pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'

jobs:

- job: backend
  displayName: Backend_Build 
  workspace:
    clean: all
  steps:
  - template: yaml/backend.yml
